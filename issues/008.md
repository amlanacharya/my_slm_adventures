---
title: Variant store add variants and match by structural signature
number: "008"
---

## What to build

Implement VariantStore in kurma_slm/src/variant_store.py.
Methods: .add(variant), .match(workflow_id, signature) -> Variant | None

Matching is exact string match on (workflow_id, signature).

## Acceptance criteria

- [ ] .match() returns Variant when both workflow_id and signature match
- [ ] Returns None when no match
- [ ] Approved and rejected variants both stored and matched
- [ ] Different workflow_id does NOT match same signature
- [ ] tests/test_variant_store.py with 4 passing tests

## User stories covered: 37,38

## Blocked by

#001