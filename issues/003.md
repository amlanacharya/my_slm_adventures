---
title: Blueprint version store save get list rollback
number: "003"
---

## What to build

Implement BlueprintVersionStore in kurma_slm/src/versioning.py.
In-memory store. Methods: .save(bp), .get(workflow_id, version), .list(workflow_id), .rollback(workflow_id, version)

## Acceptance criteria

- [ ] .save(bp) stores blueprint under workflow_id + version
- [ ] .get() raises KeyError if not found
- [ ] .list() returns versions in sorted order
- [ ] .rollback() returns the specified old blueprint
- [ ] tests/test_versioning.py with 4 passing tests

## User stories covered: 5,6,7,8

## Blocked by

#002